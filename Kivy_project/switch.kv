<MyLayout>
    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        Label:
            id: my_label
            font_size: 32
            text: "Switch is on"

        Switch:
            active: True
            font_size: 32
            on_active: root.switch_click(self, self.active)